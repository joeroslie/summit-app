---
description: Performance — file map (@ tag this chat)
alwaysApply: false
---

# Performance

**Chat name:** `Performance`  
**Sidebar label:** none — lives on Pipeline (stats + stage chips above the jobs board)  
**Agent starter:** `.cursor/agents/performance.md`

## Files

- `app/page.tsx` — `activeTab === 'leads'` rollup passed into `PipelineBoard` (total jobs, estimates, pipeline value, avg estimate above stage chips)
