---
description: Pipeline — file map (@ tag this chat)
globs: components/PipelineBoard.tsx,components/PipelineSwipeRow.tsx,lib/pipeline.ts
alwaysApply: false
---

# Pipeline

**Chat name:** `Pipeline`  
**Sidebar label:** Pipeline (`activeTab: leads`)  
**Agent starter:** `.cursor/agents/pipeline.md`

## Files

- `components/PipelineBoard.tsx` — kanban board, stage chips, trash list, New Lead header; jump chips (All → Lead → … → Closed). Finger swipe on iPhone/iPad = tap those chips; cards tap-to-open only
- `components/PipelineSwipeRow.tsx` — phone job card: tap to open (stage changes on the lead Pipeline tab; desktop kanban drag unchanged)
- `lib/pipeline.ts` — `PipelineStage`, `PIPELINE_STAGES`, `PIPELINE_VALUE_STAGES`, `PIPELINE_STAGE_STYLES`, `normalizePipelineStage`, `adjacentPipelineStage`
- `lib/phone-nav.ts` — `PIPELINE_BOARD_PAGER` / `pipelineBoardPagerStep` (finger pager; Phone chat)
- `app/page.tsx` — `activeTab === 'leads'` wires `PipelineBoard` (stats + Home-twin stage chips above the board); lead CRUD + lead profile tabs still live here. Cloud is source of truth for the lead (device cache is a scratch pad). Refresh on open, app resume, and Pipeline / Measurements pull-to-refresh. Persist only dirty leads so a stale phone copy cannot overwrite a newer save. Trash is one system: whole-lead `deleted_at` plus media/note/estimate items on `lead.trash` — not per-device `summitTrash`.
- `components/AddressAutocomplete.tsx` — shared (see App Shell)
- `components/PhoneInput.tsx` — shared (see App Shell)
- `lib/phone.ts` — shared
- `lib/geocode.ts` — shared
- `lib/supabase.ts` — shared; `leads` table
- `app/api/geocode/route.ts`
- `app/api/address-suggest/route.ts`
- `supabase/soft_delete_leads.sql`
- `supabase/enable_rls_pricing_open.sql` — shared; leads RLS
- `supabase/harden_rls_step1.sql` — shared
