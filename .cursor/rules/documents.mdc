---
description: Documents — file map (@ tag this chat)
globs: app/api/convert-heic/route.ts
alwaysApply: false
---

# Documents

**Chat name:** `Documents`  
**Sidebar label:** Documents  
**Agent starter:** `.cursor/agents/documents.md`

## Sync

- Photos, PDFs, photo reports, and mitigation invoices on a lead are one system.
- Cloud / Storage (`lead-photos`, `lead-docs`) is source of truth. Device cache is a scratch pad.
- Refresh when opening the lead Photos or Documents tab and when the app comes back. Pull-to-refresh stays as backup.

## Files

- `app/page.tsx` — `activeTab === 'documents'` (~L22465); photo reports, lead-photos/lead-docs Storage, PDF generation
- `app/api/convert-heic/route.ts`
- `components/AddressAutocomplete.tsx` — shared
- `lib/supabase.ts` — shared; Storage buckets
- `supabase/harden_rls_step1.sql` — lead-photos / lead-docs policies
- `supabase/mitigation_price_tune.sql` — shared
- `supabase/mitigation_apply_recommended.sql`
- `supabase/mitigation_tarp_install_options.sql`
- `supabase/hip_install_mitigation.sql`
- `supabase/obstruction_sandbag_mitigation.sql`
- `docs/emergency_services_agreement_prowest_az.pdf`
- `types/heic-convert.d.ts`
